# Change this env.txt and save it in /var/www/.env if your web root is /var/www/html. 
# Set permissions: sudo chmod 660 /path/to/.env
# Delete these lines with # at the beginning!
DB_HOST=127.0.0.1
DB_NAME=your_database_name
DB_USER=your_username
DB_PASS=your_password
